---
import Page from '@/layouts/Page.astro';
import PageIntro from '@/components/PageIntro.astro';
import { useTranslations } from '@/i18n';
const t = useTranslations('en');
---

<Page lang="en" route="work">
  <PageIntro
    eyebrow={t('work.eyebrow')}
    title={t('work.h1')}
    lead={t('work.lead')}
    note={t('work.note')}
  />
</Page>
